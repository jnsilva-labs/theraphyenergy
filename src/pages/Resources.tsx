import Link from "../components/LocalizedLink";
import SEO from "../components/SEO";
import useSiteContent from "../lib/useSiteContent";
import { resources } from "../content/resources";
export default function Resources() { const { locale, content } = useSiteContent(); const es = locale === "es"; return <div className="guide-page container"><SEO title={es ? "Guías para tu sesión" : "Session preparation guides"} description={es ? "Respuestas prácticas para preparar tu primera sesión, conectar online y reflexionar después." : "Practical guides for your first session, connecting online, and reflecting afterwards."} path="/resources"/><header className="guide-header"><p className="eyebrow">{es ? "Antes y después" : "Before & after"}</p><h1>{es ? "Un poco de orientación." : "A little guidance along the way."}</h1><p>{es ? "Guías sencillas para llegar con más claridad y seguir reflexionando a tu ritmo." : "Simple guides to help you arrive with more clarity and keep reflecting at your own pace."}</p></header><div className="guide-grid">{resources.map((r, i) => <article key={r.slug}><p className="eyebrow">0{i + 1} / {es ? "Guía de sesión" : "Session guide"}</p><h2><Link to={`/resources/${r.slug}`}>{r[locale].title}</Link></h2><p>{r[locale].intro}</p><Link className="text-link" to={`/resources/${r.slug}`}>{es ? "Leer la guía" : "Read the guide"} ↗</Link></article>)}</div><section className="guide-service-list"><h2>{es ? "Prepara tu sesión" : "Prepare for your session"}</h2>{content.services.map(s => <Link key={s.slug} to={`/prepare/${s.slug}`}>{s.title}<span aria-hidden="true">↗</span></Link>)}</section></div>; }
